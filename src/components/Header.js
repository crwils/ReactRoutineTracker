import React from 'react';
import { Typography } from '@mui/material';

function Header(props) {
    return (
        <div>
            <h1 className="center heading"><Typography variant="h2" component="h2">Routine Tracker</Typography></h1>
            
        </div>
    );
}


export default Header;